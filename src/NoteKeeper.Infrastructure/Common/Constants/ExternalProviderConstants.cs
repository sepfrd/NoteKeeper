namespace NoteKeeper.Infrastructure.Common.Constants;

public static class ExternalProviderConstants
{
    public const string Notion = nameof(Notion);
    public const string Google = nameof(Google);
}